{
  "name": "immsfront",
  "version": "3.0.0",
  "description": "descripción",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "amg build -e DIST_PATH=../react --api-path /react-api --base-path /",
    "lint": "amg lint",
    "format": "amg format -w",
    "test": "amg test",
    "generate": "amg generate",
    "start": "amg server --https --api-path /react-api --base-path / --disable-json-api",
    "amiga-docs": "amg amiga-docs"
  },
  "engines": {
    "node": ">= 12.10.x < 13.x",
    "npm": ">= 6.10.x",
    "yarn": ">= 1.19.0"
  },
  "devDependencies": {
    "amiga": "7.3.0",
    "amiga-tools": "7.3.0",
    "env-dot-prop": "^3.0.0"
  },
  "dependencies": {
    "@phuocng/react-pdf-viewer": "^1.7.0",
    "@rsql/builder": "1.0.4",
    "@rsql/emitter": "1.0.4",
    "lib-frontsga": "6.6.1",
    "ramda": "0.28.0",
    "react-custom-scrollbars": "4.2.1",
    "react-drag-and-drop": "3.0.0",
    "react-qr-reader": "^3.0.0-beta-1"
  },
  "eslintConfig": {
    "extends": "amiga",
    "react/display-name": 0
  }
}
